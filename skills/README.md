# Skills

Each subfolder here is a standalone Claude skill (a `SKILL.md`, optionally with supporting
scripts/templates). Copy `templates/skill-template/` to start a new one.

A skill folder should contain only generic, reusable instructions — no company names,
internal tool references, or credentials. If the skill was originally built for a specific
employer's workflow, rewrite the examples to be generic before adding it here.
